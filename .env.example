# =============================================================================
# Code Review Agent - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# The application loads .env file first, then falls back to system variables
#
# Priority: .env file > System Environment Variables > Default Values
# =============================================================================

# -----------------------------------------------------------------------------
# AI Provider Configuration (Choose ONE option)
# -----------------------------------------------------------------------------

# Option 1: Azure OpenAI (Recommended for Enterprise)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_DEPLOYMENT=gpt-4

# Optional: Separate embeddings configuration (if using different resource)
# AZURE_OPENAI_EMBEDDING_ENDPOINT=https://your-embedding-resource.openai.azure.com/
# AZURE_OPENAI_EMBEDDING_API_KEY=your-embedding-api-key
# AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-ada-002

# Option 2: OpenAI (Alternative)
# Uncomment the line below if using OpenAI instead of Azure
# OPENAI_API_KEY=sk-your-openai-api-key

# -----------------------------------------------------------------------------
# Azure DevOps Configuration (Required)
# -----------------------------------------------------------------------------
ADO_PAT=your-personal-access-token
ADO_ORGANIZATION=your-organization-name

# -----------------------------------------------------------------------------
# Optional Configuration
# -----------------------------------------------------------------------------
# MCP_SERVER_URL=http://localhost:3000
